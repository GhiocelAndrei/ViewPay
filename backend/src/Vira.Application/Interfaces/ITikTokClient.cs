namespace Vira.Application.Interfaces;

/// <summary>TikTok Display API contract (Login Kit + video.list + video.query). TODO: define methods.</summary>
public interface ITikTokClient
{
    // e.g. Task<TikTokTokens> ExchangeCodeAsync(...);  — added later
}
